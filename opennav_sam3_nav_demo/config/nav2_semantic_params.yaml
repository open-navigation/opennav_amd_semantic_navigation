bt_navigator:
  ros__parameters:
    global_frame: map
    robot_base_frame: base_link
    odom_topic: /platform/odom/filtered
    bt_loop_duration: 10
    default_server_timeout: 20
    wait_for_service_timeout: 1000
    action_server_result_timeout: 900.0
    navigators: ["navigate_to_pose", "navigate_through_poses"]
    navigate_to_pose:
      plugin: "nav2_bt_navigator::NavigateToPoseNavigator"
    navigate_through_poses:
      plugin: "nav2_bt_navigator::NavigateThroughPosesNavigator"
    # 'default_nav_through_poses_bt_xml' and 'default_nav_to_pose_bt_xml' are use defaults:
    # nav2_bt_navigator/navigate_to_pose_w_replanning_and_recovery.xml
    # nav2_bt_navigator/navigate_through_poses_w_replanning_and_recovery.xml
    # They can be set here or via a RewrittenYaml remap from a parent launch file to Nav2.

    # plugin_lib_names is used to add custom BT plugins to the executor (vector of strings).
    # Built-in plugins are added automatically
    # plugin_lib_names: []

    error_code_names:
      - compute_path_error_code
      - follow_path_error_code

controller_server:
  ros__parameters:
    controller_frequency: 20.0
    costmap_update_timeout: 0.30
    min_x_velocity_threshold: 0.05
    min_y_velocity_threshold: 0.5
    min_theta_velocity_threshold: 0.01
    enable_stamped_cmd_vel: true
    failure_tolerance: 0.5
    progress_checker_plugins: ["progress_checker"]
    goal_checker_plugins: ["general_goal_checker"]
    controller_plugins: ["FollowPath"]
    use_realtime_priority: false

    # Progress checker parameters
    progress_checker:
      plugin: "nav2_controller::SimpleProgressChecker"
      required_movement_radius: 0.5
      movement_time_allowance: 10.0

    # Goal checker parameters
    general_goal_checker:
      stateful: True
      plugin: "nav2_controller::SimpleGoalChecker"
      xy_goal_tolerance: 0.3
      yaw_goal_tolerance: 0.2

    # Controller parameters
    FollowPath:
      plugin: "nav2_regulated_pure_pursuit_controller::RegulatedPurePursuitController"
      max_linear_vel: 0.5
      min_linear_vel: -0.5
      max_angular_vel: 2.5
      min_angular_vel: -2.5
      max_linear_accel: 2.5
      max_linear_decel: -2.5
      max_angular_accel: 3.2
      max_angular_decel: -3.2
      lookahead_dist: 0.6
      min_lookahead_dist: 0.3
      max_lookahead_dist: 0.9
      lookahead_time: 1.5
      rotate_to_heading_angular_vel: 1.8
      use_velocity_scaled_lookahead_dist: false
      min_approach_linear_velocity: 0.05
      approach_velocity_scaling_dist: 0.6
      use_collision_detection: true
      max_allowed_time_to_collision_up_to_carrot: 1.0
      use_regulated_linear_velocity_scaling: true
      use_fixed_curvature_lookahead: false
      curvature_lookahead_dist: 0.25
      use_cost_regulated_linear_velocity_scaling: false
      cost_scaling_dist: 0.3
      cost_scaling_gain: 1.0
      regulated_linear_scaling_min_radius: 0.9
      regulated_linear_scaling_min_speed: 0.25
      use_rotate_to_heading: true
      allow_reversing: false
      rotate_to_heading_min_angle: 0.785
      min_distance_to_obstacle: 0.0
      use_dynamic_window: false
      allow_obstacle_checking_beyond_goal: false

local_costmap:
  local_costmap:
    ros__parameters:
      update_frequency: 5.0
      publish_frequency: 5.0
      global_frame: odom
      robot_base_frame: base_link
      rolling_window: true
      width: 4
      height: 4
      resolution: 0.05
      footprint: "[ [0.254, 0.2159], [0.254, -0.2159], [-0.254, -0.2159], [-0.254, 0.2159] ]"
      footprint_padding: 0.1
      plugins: ["semantic_segmentation_layer", "inflation_layer"] # voxel layer
      inflation_layer:
        plugin: "nav2_costmap_2d::InflationLayer"
        cost_scaling_factor: 3.0
        inflation_radius: 2.0
      # Disabled for semantic-only navigation!
      # voxel_layer:
      #   plugin: "nav2_costmap_2d::VoxelLayer"
      #   enabled: True
      #   publish_voxel_map: False
      #   footprint_clearing_enabled: True
      #   origin_z: 0.0
      #   z_resolution: 0.05
      #   z_voxels: 12
      #   max_obstacle_height: 2.0
      #   mark_threshold: 0
      #   observation_sources: lidar depth_cam
      #   lidar:
      #     topic: /sensors/lidar_0/scan
      #     min_obstacle_height: 0.254
      #     max_obstacle_height: 0.8
      #     clearing: True
      #     marking: True
      #     data_type: "LaserScan"
      #     raytrace_max_range: 5.65  # max diagonal of 4m costmap
      #     raytrace_min_range: 0.0
      #     obstacle_max_range: 4.0   # 4m costmap range maximum
      #     obstacle_min_range: 0.2
      #   depth_cam:
      #     topic: /sensors/camera_0/points
      #     min_obstacle_height: 0.1524
      #     max_obstacle_height: 0.8
      #     clearing: True
      #     marking: True
      #     data_type: "PointCloud2"
      #     raytrace_max_range: 3.0
      #     raytrace_min_range: 0.0
      #     obstacle_max_range: 2.5
      #     obstacle_min_range: 0.2
      static_layer:
        plugin: "nav2_costmap_2d::StaticLayer"
        map_subscribe_transient_local: True
      semantic_segmentation_layer:
        plugin: "semantic_segmentation_layer::SemanticSegmentationLayer"
        enabled: True
        observation_sources: camera
        camera:
          segmentation_topic: "/sam3_inference/resized/label_mask"
          # confidence_topic: "/sam3_inference/label_mask"
          labels_topic: "/sam3_inference/label_info"
          pointcloud_topic: "/sensors/camera_0/points_registered"
          observation_persistence: 0.0
          expected_update_rate: 0.0
          visualize_tile_map: True
          use_cost_selection: True  # If true, select observation per tile using highest max_cost in case multile observations fall in a single tile on one image. If false, use highest confidence
          max_obstacle_distance: 4.0
          min_obstacle_distance: 0.3
          tile_map_decay_time: 1.5

          class_types: ["traversable"]
          traversable:
            classes: ["floor"]
            base_cost: 15              # Cost when a single observation is received
            max_cost: 30               # Cost when samples_to_max_cost observations with mark_confidence are accumulated
            mark_confidence: 0        # Average confidence threshold to apply max_cost
            samples_to_max_cost: 100    # Number of observations needed to apply max_cost
            dominant_priority: False  # If true, this class immediately becomes dominant and clears all other class queues from the tile

          # class_types: ["traversable", "moderate1", "moderate2", "avoid"]
          # traversable:
          #   classes: ["floor"]
          #   base_cost: 15              # Cost when a single observation is received
          #   max_cost: 30               # Cost when samples_to_max_cost observations with mark_confidence are accumulated
          #   mark_confidence: 0        # Average confidence threshold to apply max_cost
          #   samples_to_max_cost: 100    # Number of observations needed to apply max_cost
          #   dominant_priority: False  # If true, this class immediately becomes dominant and clears all other class queues from the tile
          # moderate1:
          #   classes: ["shelf and cabinet"]
          #   base_cost: 75            # Cost when a single observation is received
          #   max_cost: 100             # Cost when samples_to_max_cost observations with mark_confidence are accumulated
          #   mark_confidence: 0        # Average confidence threshold to apply max_cost
          #   samples_to_max_cost: 50    # Number of observations needed to apply max_cost
          #   dominant_priority: False  # If true, this class immediately becomes dominant and clears all other class queues from the tile
          # moderate2:
          #   classes: ["desk", "chair"]
          #   base_cost: 150            # Cost when a single observation is received
          #   max_cost: 175             # Cost when samples_to_max_cost observations with mark_confidence are accumulated
          #   mark_confidence: 0        # Average confidence threshold to apply max_cost
          #   samples_to_max_cost: 50    # Number of observations needed to apply max_cost
          #   dominant_priority: False  # If true, this class immediately becomes dominant and clears all other class queues from the tile
          # avoid:
          #   classes: ["wall"]
          #   base_cost: 225            # Cost when a single observation is received
          #   max_cost: 254             # Cost when samples_to_max_cost observations with mark_confidence are accumulated
          #   mark_confidence: 0        # Average confidence threshold to apply max_cost
          #   samples_to_max_cost: 100    # Number of observations needed to apply max_cost
          #   dominant_priority: False  # If true, this class immediately becomes dominant and clears all other class queues from the tile

          # class_types: ["preferred", "cautious", "avoid"]
          # preferred:
          #   classes: ["paved cement"]
          #   base_cost: 15              # Cost when a single observation is received
          #   max_cost: 30               # Cost when samples_to_max_cost observations with mark_confidence are accumulated
          #   mark_confidence: 0        # Average confidence threshold to apply max_cost
          #   samples_to_max_cost: 100    # Number of observations needed to apply max_cost
          #   dominant_priority: False  # If true, this class immediately becomes dominant and clears all other class queues from the tile
          # cautious:
          #   classes: ["asphalt road"]
          #   base_cost: 75            # Cost when a single observation is received
          #   max_cost: 100             # Cost when samples_to_max_cost observations with mark_confidence are accumulated
          #   mark_confidence: 0        # Average confidence threshold to apply max_cost
          #   samples_to_max_cost: 100    # Number of observations needed to apply max_cost
          #   dominant_priority: False  # If true, this class immediately becomes dominant and clears all other class queues from the tile
          # avoid:
          #   classes: ["grass field"]
          #   base_cost: 254            # Cost when a single observation is received
          #   max_cost: 254             # Cost when samples_to_max_cost observations with mark_confidence are accumulated
          #   mark_confidence: 0        # Average confidence threshold to apply max_cost
          #   samples_to_max_cost: 75    # Number of observations needed to apply max_cost
          #   dominant_priority: False  # If true, this class immediately becomes dominant and clears all other class queues from the tile

          # class_types: ["traversable", "avoid"]
          # traversable:
          #   classes: ["paved cement sidewalk"]
          #   base_cost: 15              # Cost when a single observation is received
          #   max_cost: 30               # Cost when samples_to_max_cost observations with mark_confidence are accumulated
          #   mark_confidence: 0        # Average confidence threshold to apply max_cost
          #   samples_to_max_cost: 100    # Number of observations needed to apply max_cost
          #   dominant_priority: False  # If true, this class immediately becomes dominant and clears all other class queues from the tile
          # avoid:
          #   classes: ["grass field"]
          #   base_cost: 254            # Cost when a single observation is received
          #   max_cost: 254             # Cost when samples_to_max_cost observations with mark_confidence are accumulated
          #   mark_confidence: 0        # Average confidence threshold to apply max_cost
          #   samples_to_max_cost: 75    # Number of observations needed to apply max_cost
          #   dominant_priority: False  # If true, this class immediately becomes dominant and clears all other class queues from the tile

          # class_types: ["traversable", "moderate", "avoid"]
          # traversable:
          #   classes: ["asphalt lane", "bike lane"]
          #   base_cost: 15              # Cost when a single observation is received
          #   max_cost: 30               # Cost when samples_to_max_cost observations with mark_confidence are accumulated
          #   mark_confidence: 0        # Average confidence threshold to apply max_cost
          #   samples_to_max_cost: 100    # Number of observations needed to apply max_cost
          #   dominant_priority: False  # If true, this class immediately becomes dominant and clears all other class queues from the tile
          # moderate:
          #   classes: ["sidewalk"]
          #   base_cost: 75            # Cost when a single observation is received
          #   max_cost: 100             # Cost when samples_to_max_cost observations with mark_confidence are accumulated
          #   mark_confidence: 0        # Average confidence threshold to apply max_cost
          #   samples_to_max_cost: 75    # Number of observations needed to apply max_cost
          #   dominant_priority: False  # If true, this class immediately becomes dominant and clears all other class queues from the tile
          # avoid:
          #   classes: ["bushes and plants"]
          #   base_cost: 254            # Cost when a single observation is received
          #   max_cost: 254             # Cost when samples_to_max_cost observations with mark_confidence are accumulated
          #   mark_confidence: 0        # Average confidence threshold to apply max_cost
          #   samples_to_max_cost: 75    # Number of observations needed to apply max_cost
          #   dominant_priority: False  # If true, this class immediately becomes dominant and clears all other class queues from the tile

          # class_types: ["traversable", "moderate", "avoid"]
          # traversable:
          #   classes: ["paved cement"]
          #   base_cost: 15              # Cost when a single observation is received
          #   max_cost: 30               # Cost when samples_to_max_cost observations with mark_confidence are accumulated
          #   mark_confidence: 0        # Average confidence threshold to apply max_cost
          #   samples_to_max_cost: 100    # Number of observations needed to apply max_cost
          #   dominant_priority: False  # If true, this class immediately becomes dominant and clears all other class queues from the tile
          # moderate:
          #   classes: ["tree"]
          #   base_cost: 75            # Cost when a single observation is received
          #   max_cost: 100             # Cost when samples_to_max_cost observations with mark_confidence are accumulated
          #   mark_confidence: 0        # Average confidence threshold to apply max_cost
          #   samples_to_max_cost: 75    # Number of observations needed to apply max_cost
          #   dominant_priority: False  # If true, this class immediately becomes dominant and clears all other class queues from the tile
          # avoid:
          #   classes: ["car"]
          #   base_cost: 254            # Cost when a single observation is received
          #   max_cost: 254             # Cost when samples_to_max_cost observations with mark_confidence are accumulated
          #   mark_confidence: 0        # Average confidence threshold to apply max_cost
          #   samples_to_max_cost: 75    # Number of observations needed to apply max_cost
          #   dominant_priority: False  # If true, this class immediately becomes dominant and clears all other class queues from the tile

      always_send_full_costmap: True

global_costmap:
  global_costmap:
    ros__parameters:
      update_frequency: 2.0
      publish_frequency: 1.0
      width: 200
      height: 200
      origin_x: -100.0
      origin_y: -100.0
      global_frame: map
      robot_base_frame: base_link
      footprint: "[ [0.254, 0.2159], [0.254, -0.2159], [-0.254, -0.2159], [-0.254, 0.2159] ]"
      robot_radius: 0.333
      footprint_padding: 0.1
      resolution: 0.05
      track_unknown_space: true
      plugins: ["semantic_segmentation_layer", "inflation_layer"] # voxel layer
      # Disabled for semantic-only navigation!
      # voxel_layer:
      #   plugin: "nav2_costmap_2d::VoxelLayer"
      #   enabled: True
      #   publish_voxel_map: False
      #   footprint_clearing_enabled: True
      #   origin_z: 0.0
      #   z_resolution: 0.05
      #   z_voxels: 12
      #   max_obstacle_height: 2.0
      #   mark_threshold: 0
      #   observation_sources: lidar depth_cam
      #   lidar:
      #     topic: /sensors/lidar_0/scan
      #     min_obstacle_height: 0.254
      #     max_obstacle_height: 0.8
      #     clearing: True
      #     marking: True
      #     data_type: "LaserScan"
      #     raytrace_max_range: 11.0
      #     raytrace_min_range: 0.0
      #     obstacle_max_range: 10.0
      #     obstacle_min_range: 0.2
      #   depth_cam:
      #     topic: /sensors/camera_0/points
      #     min_obstacle_height: 0.1524
      #     max_obstacle_height: 0.8
      #     clearing: True
      #     marking: True
      #     data_type: "PointCloud2"
      #     raytrace_max_range: 3.0
      #     raytrace_min_range: 0.0
      #     obstacle_max_range: 2.5
      #     obstacle_min_range: 0.2
      static_layer:
        plugin: "nav2_costmap_2d::StaticLayer"
        map_subscribe_transient_local: True
      inflation_layer:
        plugin: "nav2_costmap_2d::InflationLayer"
        cost_scaling_factor: 3.0
        inflation_radius: 2.0
      semantic_segmentation_layer:
        plugin: "semantic_segmentation_layer::SemanticSegmentationLayer"
        enabled: True
        observation_sources: camera
        camera:
          segmentation_topic: "/sam3_inference/resized/label_mask"
          # confidence_topic: "/sam3_inference/label_mask"
          labels_topic: "/sam3_inference/label_info"
          pointcloud_topic: "/sensors/camera_0/points_registered"
          observation_persistence: 0.0
          expected_update_rate: 0.0
          visualize_tile_map: True
          use_cost_selection: True  # If true, select observation per tile using highest max_cost in case multile observations fall in a single tile on one image. If false, use highest confidence
          max_obstacle_distance: 4.0
          min_obstacle_distance: 0.3
          tile_map_decay_time: 3.0

          class_types: ["traversable"]
          traversable:
            classes: ["floor"]
            base_cost: 15              # Cost when a single observation is received
            max_cost: 30               # Cost when samples_to_max_cost observations with mark_confidence are accumulated
            mark_confidence: 0        # Average confidence threshold to apply max_cost
            samples_to_max_cost: 100    # Number of observations needed to apply max_cost
            dominant_priority: False  # If true, this class immediately becomes dominant and clears all other class queues from the tile

          # class_types: ["traversable", "moderate1", "moderate2", "avoid"]
          # traversable:
          #   classes: ["floor"]
          #   base_cost: 15              # Cost when a single observation is received
          #   max_cost: 30               # Cost when samples_to_max_cost observations with mark_confidence are accumulated
          #   mark_confidence: 0        # Average confidence threshold to apply max_cost
          #   samples_to_max_cost: 100    # Number of observations needed to apply max_cost
          #   dominant_priority: False  # If true, this class immediately becomes dominant and clears all other class queues from the tile
          # moderate1:
          #   classes: ["shelf and cabinet"]
          #   base_cost: 75            # Cost when a single observation is received
          #   max_cost: 100             # Cost when samples_to_max_cost observations with mark_confidence are accumulated
          #   mark_confidence: 0        # Average confidence threshold to apply max_cost
          #   samples_to_max_cost: 50    # Number of observations needed to apply max_cost
          #   dominant_priority: False  # If true, this class immediately becomes dominant and clears all other class queues from the tile
          # moderate2:
          #   classes: ["desk", "chair"]
          #   base_cost: 150            # Cost when a single observation is received
          #   max_cost: 175             # Cost when samples_to_max_cost observations with mark_confidence are accumulated
          #   mark_confidence: 0        # Average confidence threshold to apply max_cost
          #   samples_to_max_cost: 50    # Number of observations needed to apply max_cost
          #   dominant_priority: False  # If true, this class immediately becomes dominant and clears all other class queues from the tile
          # avoid:
          #   classes: ["wall"]
          #   base_cost: 225            # Cost when a single observation is received
          #   max_cost: 254             # Cost when samples_to_max_cost observations with mark_confidence are accumulated
          #   mark_confidence: 0        # Average confidence threshold to apply max_cost
          #   samples_to_max_cost: 100    # Number of observations needed to apply max_cost
          #   dominant_priority: False  # If true, this class immediately becomes dominant and clears all other class queues from the tile

          # class_types: ["preferred", "cautious", "avoid"]
          # preferred:
          #   classes: ["paved cement"]
          #   base_cost: 15              # Cost when a single observation is received
          #   max_cost: 30               # Cost when samples_to_max_cost observations with mark_confidence are accumulated
          #   mark_confidence: 0        # Average confidence threshold to apply max_cost
          #   samples_to_max_cost: 100    # Number of observations needed to apply max_cost
          #   dominant_priority: False  # If true, this class immediately becomes dominant and clears all other class queues from the tile
          # cautious:
          #   classes: ["asphalt road"]
          #   base_cost: 75            # Cost when a single observation is received
          #   max_cost: 100             # Cost when samples_to_max_cost observations with mark_confidence are accumulated
          #   mark_confidence: 0        # Average confidence threshold to apply max_cost
          #   samples_to_max_cost: 100    # Number of observations needed to apply max_cost
          #   dominant_priority: False  # If true, this class immediately becomes dominant and clears all other class queues from the tile
          # avoid:
          #   classes: ["grass field"]
          #   base_cost: 254            # Cost when a single observation is received
          #   max_cost: 254             # Cost when samples_to_max_cost observations with mark_confidence are accumulated
          #   mark_confidence: 0        # Average confidence threshold to apply max_cost
          #   samples_to_max_cost: 75    # Number of observations needed to apply max_cost
          #   dominant_priority: False  # If true, this class immediately becomes dominant and clears all other class queues from the tile

          # class_types: ["traversable", "avoid"]
          # traversable:
          #   classes: ["paved cement sidewalk"]
          #   base_cost: 15              # Cost when a single observation is received
          #   max_cost: 30               # Cost when samples_to_max_cost observations with mark_confidence are accumulated
          #   mark_confidence: 0        # Average confidence threshold to apply max_cost
          #   samples_to_max_cost: 100    # Number of observations needed to apply max_cost
          #   dominant_priority: False  # If true, this class immediately becomes dominant and clears all other class queues from the tile
          # avoid:
          #   classes: ["grass field"]
          #   base_cost: 254            # Cost when a single observation is received
          #   max_cost: 254             # Cost when samples_to_max_cost observations with mark_confidence are accumulated
          #   mark_confidence: 0        # Average confidence threshold to apply max_cost
          #   samples_to_max_cost: 75    # Number of observations needed to apply max_cost
          #   dominant_priority: False  # If true, this class immediately becomes dominant and clears all other class queues from the tile

          # class_types: ["traversable", "moderate", "avoid"]
          # traversable:
          #   classes: ["asphalt lane", "bike lane"]
          #   base_cost: 15              # Cost when a single observation is received
          #   max_cost: 30               # Cost when samples_to_max_cost observations with mark_confidence are accumulated
          #   mark_confidence: 0        # Average confidence threshold to apply max_cost
          #   samples_to_max_cost: 100    # Number of observations needed to apply max_cost
          #   dominant_priority: False  # If true, this class immediately becomes dominant and clears all other class queues from the tile
          # moderate:
          #   classes: ["sidewalk"]
          #   base_cost: 75            # Cost when a single observation is received
          #   max_cost: 100             # Cost when samples_to_max_cost observations with mark_confidence are accumulated
          #   mark_confidence: 0        # Average confidence threshold to apply max_cost
          #   samples_to_max_cost: 75    # Number of observations needed to apply max_cost
          #   dominant_priority: False  # If true, this class immediately becomes dominant and clears all other class queues from the tile
          # avoid:
          #   classes: ["bushes and plants"]
          #   base_cost: 254            # Cost when a single observation is received
          #   max_cost: 254             # Cost when samples_to_max_cost observations with mark_confidence are accumulated
          #   mark_confidence: 0        # Average confidence threshold to apply max_cost
          #   samples_to_max_cost: 75    # Number of observations needed to apply max_cost
          #   dominant_priority: False  # If true, this class immediately becomes dominant and clears all other class queues from the tile

          # class_types: ["traversable", "moderate", "avoid"]
          # traversable:
          #   classes: ["paved cement"]
          #   base_cost: 15              # Cost when a single observation is received
          #   max_cost: 30               # Cost when samples_to_max_cost observations with mark_confidence are accumulated
          #   mark_confidence: 0        # Average confidence threshold to apply max_cost
          #   samples_to_max_cost: 100    # Number of observations needed to apply max_cost
          #   dominant_priority: False  # If true, this class immediately becomes dominant and clears all other class queues from the tile
          # moderate:
          #   classes: ["tree"]
          #   base_cost: 75            # Cost when a single observation is received
          #   max_cost: 100             # Cost when samples_to_max_cost observations with mark_confidence are accumulated
          #   mark_confidence: 0        # Average confidence threshold to apply max_cost
          #   samples_to_max_cost: 75    # Number of observations needed to apply max_cost
          #   dominant_priority: False  # If true, this class immediately becomes dominant and clears all other class queues from the tile
          # avoid:
          #   classes: ["car"]
          #   base_cost: 254            # Cost when a single observation is received
          #   max_cost: 254             # Cost when samples_to_max_cost observations with mark_confidence are accumulated
          #   mark_confidence: 0        # Average confidence threshold to apply max_cost
          #   samples_to_max_cost: 75    # Number of observations needed to apply max_cost
          #   dominant_priority: False  # If true, this class immediately becomes dominant and clears all other class queues from the tile

      always_send_full_costmap: True

# map_server:
#   ros__parameters:
#     yaml_filename: ""

map_saver:
  ros__parameters:
    save_map_timeout: 5.0
    free_thresh_default: 0.25
    occupied_thresh_default: 0.65
    map_subscribe_transient_local: True

planner_server:
  ros__parameters:
    expected_planner_frequency: 5.0
    planner_plugins: ["GridBased"]
    GridBased:
      plugin: "nav2_smac_planner::SmacPlannerHybrid"
      downsample_costmap: false           # whether or not to downsample the map
      downsampling_factor: 1              # multiplier for the resolution of the costmap layer (e.g. 2 on a 5cm costmap would be 10cm)
      tolerance: 0.25                     # dist-to-goal heuristic cost (distance) for valid tolerance endpoints if exact goal cannot be found.
      allow_unknown: true                 # allow traveling in unknown space
      max_iterations: 1000000             # maximum total iterations to search for before failing (in case unreachable), set to -1 to disable
      max_on_approach_iterations: 1000    # Maximum number of iterations after within tolerances to continue to try to find exact solution
      max_planning_time: 5.0              # max time in s for planner to plan, smooth
      motion_model_for_search: "REEDS_SHEPP"    # Hybrid-A* Dubin, Redds-Shepp
      angle_quantization_bins: 72         # Number of angle bins for search
      analytic_expansion_ratio: 3.5       # The ratio to attempt analytic expansions during search for final approach.
      analytic_expansion_max_length: 3.0  # For Hybrid/Lattice nodes: The maximum length of the analytic expansion to be considered valid to prevent unsafe shortcutting
      analytic_expansion_max_cost: 200.0  # The maximum single cost for any part of an analytic expansion to contain and be valid, except when necessary on approach to goal
      analytic_expansion_max_cost_override: false  #  Whether or not to override the maximum cost setting if within critical distance to goal (ie probably required)
      minimum_turning_radius: 0.40        # minimum turning radius in m of path / vehicle
      reverse_penalty: 2.0                # Penalty to apply if motion is reversing, must be => 1
      change_penalty: 0.0                 # Penalty to apply if motion is changing directions (L to R), must be >= 0
      non_straight_penalty: 1.2           # Penalty to apply if motion is non-straight, must be => 1
      cost_penalty: 2.0                   # Penalty to apply to higher cost areas when adding into the obstacle map dynamic programming distance expansion heuristic. This drives the robot more towards the center of passages. A value between 1.3 - 3.5 is reasonable.
      retrospective_penalty: 0.015
      lookup_table_size: 5.0              # Size of the dubin/reeds-sheep distance window to cache, in meters.
      cache_obstacle_heuristic: false     # Cache the obstacle map dynamic programming distance expansion heuristic between subsiquent replannings of the same goal location. Dramatically speeds up replanning performance (40x) if costmap is largely static.
      debug_visualizations: false         # For Hybrid nodes: Whether to publish expansions on the /expansions topic as an array of poses (the orientation has no meaning) and the path's footprints on the /planned_footprints topic. WARNING: heavy to compute and to display, for debug only as it degrades the performance.
      use_quadratic_cost_penalty: False
      downsample_obstacle_heuristic: True
      allow_primitive_interpolation: False
      smooth_path: True                   # If true, does a simple and quick smoothing post-processing to the path

      smoother:
        max_iterations: 1000
        w_smooth: 0.3
        w_data: 0.2
        tolerance: 1.0e-10
        do_refinement: true
        refinement_num: 2

smoother_server:
  ros__parameters:
    smoother_plugins: ["simple_smoother"]
    simple_smoother:
      plugin: "nav2_smoother::SimpleSmoother"
      tolerance: 1.0e-10
      max_its: 1000
      do_refinement: True

behavior_server:
  ros__parameters:
    local_costmap_topic: local_costmap/costmap_raw
    local_footprint_topic: local_costmap/published_footprint
    cycle_frequency: 10.0
    behavior_plugins: ["spin", "backup", "drive_on_heading", "assisted_teleop", "wait"]
    spin:
      plugin: "nav2_behaviors::Spin"
    backup:
      plugin: "nav2_behaviors::BackUp"
    drive_on_heading:
      plugin: "nav2_behaviors::DriveOnHeading"
    wait:
      plugin: "nav2_behaviors::Wait"
    assisted_teleop:
      plugin: "nav2_behaviors::AssistedTeleop"
    global_frame: odom
    robot_base_frame: base_link
    transform_tolerance: 0.1
    simulate_ahead_time: 2.0
    max_rotational_vel: 1.0
    min_rotational_vel: 0.6
    rotational_acc_lim: 3.5
    enable_stamped_cmd_vel: true

waypoint_follower:
  ros__parameters:
    loop_rate: 20
    stop_on_failure: false
    action_server_result_timeout: 900.0
    waypoint_task_executor_plugin: "wait_at_waypoint"
    wait_at_waypoint:
      plugin: "nav2_waypoint_follower::WaitAtWaypoint"
      enabled: True
      waypoint_pause_duration: 3000

velocity_smoother:
  ros__parameters:
    smoothing_frequency: 20.0
    stamp_smoothed_velocity_with_smoothing_time: True
    scale_velocities: True
    feedback: "OPEN_LOOP"
    max_velocity: [0.5, 0.0, 1.4]
    min_velocity: [-0.5, 0.0, -1.4]
    max_accel: [3.5, 0.0, 20.0]
    max_decel: [-3.5, 0.0, -20.0]
    odom_topic: "/platform/odom/filtered"
    odom_duration: 0.1
    deadband_velocity: [0.0, 0.0, 0.0]
    velocity_timeout: 1.0
    enable_stamped_cmd_vel: true

collision_monitor:
  ros__parameters:
    base_frame_id: "base_link"
    odom_frame_id: "odom"
    cmd_vel_in_topic: "cmd_vel_smoothed"
    cmd_vel_out_topic: "cmd_vel"
    enable_stamped_cmd_vel: true
    state_topic: "collision_monitor_state"
    transform_tolerance: 0.2
    source_timeout: 2.0
    base_shift_correction: False
    stop_pub_timeout: 2.0
    polygons: ["FootprintApproach"]
    FootprintApproach:
      type: "polygon"
      action_type: "approach"
      footprint_topic: "/local_costmap/published_footprint"
      time_before_collision: 1.5
      simulation_time_step: 0.1
      min_points: 4
      visualize: True
      enabled: False
    observation_sources: ["scan"]
    scan:
      type: "scan"
      topic: "/sensors/lidar_0/scan"
      min_height: 0.15
      max_height: 0.4
      enabled: True
